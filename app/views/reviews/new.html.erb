<%= simple_form_for [ @restaurant, @review] do |f| %>
  <%= f.input :rating, as: :select,  collection: 0..5 %>
  <%= f.input :content %>
  <%= f.submit "Publish Review", class: "btn btn-info"%>
<% end %>
